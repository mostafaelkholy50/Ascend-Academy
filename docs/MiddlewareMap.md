# Middleware Map

This map outlines the verified custom Middleware in the system.

## Verified Middleware
**None.** 

The directory `app/Http/Middleware` does not exist. 
*Note: In modern Laravel 11+ applications, default middleware is often stripped from this directory and managed in `bootstrap/app.php`. The project heavily relies on the external `spatie/laravel-permission` middleware aliases configured globally.*

---
**Last Scan Date:** June 2026
**Analyzed Files Count:** 0

## Source References
- `app/Http/Middleware/` (Directory Not Found)
- `bootstrap/app.php`
