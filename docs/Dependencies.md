# Dependencies & Packages

Core packages currently in use:
- `laravel/framework` (^12.0)
- `spatie/laravel-permission` (Role/Permission management)
- TailwindCSS, PostCSS, Vite (Frontend asset building)

Always check `composer.json` and `package.json` for the full, current list. Do not introduce new heavy dependencies without architectural review.
