# Job Map

This map outlines the verified Jobs in the `app/Jobs` directory.

## Verified Jobs
**None.** 

The directory `app/Jobs` does not exist. The application handles asynchronous tasks via scheduled `Commands` and queued `Notifications` (which implement `ShouldQueue`), rather than dedicated Job classes.

---
**Last Scan Date:** June 2026
**Analyzed Files Count:** 0

## Source References
- `app/Jobs/` (Directory Not Found)
