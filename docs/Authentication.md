# Authentication Strategy

- Managed via Laravel's built-in session/cookie based authentication for web routes.
- **Rule**: Authentication checks must be applied at the route middleware level (`auth`).
